# Copy this file to .env and fill in your real values for local development.
# In Vercel, set these same two under Project Settings -> Environment Variables instead —
# .env itself is gitignored and never gets committed or deployed.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_your_key_here
